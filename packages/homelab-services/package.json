{
  "name": "homelab-services",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "description": "Runtime-agnostic service definitions, schemas, and types for homelab",
  "exports": {
    ".": {
      "import": "./src/index.ts"
    },
    "./resource-uris": {
      "import": "./src/resource-uris.ts"
    }
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc -b tsconfig.json --noEmit",
    "test": "vitest run --pass-with-no-tests",
    "circular": "yarn madge --circular ./src/index.ts",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@effect/platform": "^0.96.x",
    "effect": "^3.21.x",
    "homelab-shared": "workspace:*",
    "jose": "^6.2.1"
  },
  "devDependencies": {
    "@effect/vitest": "^0.29.x",
    "@types/node": "^24",
    "madge": "^8.0.0",
    "vitest": "^3.x"
  }
}
